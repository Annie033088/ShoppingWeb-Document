循環引用 指的是兩個或多個對象之間互相引用對方，這會在一些情況下引發問題。例如，假設你有兩個實體 A 和 B，A 引用了 B，而 B 又引用了 A，這樣就會造成無限循環。


當你的應用程式中有複雜的資料結構時，尤其是在分層架構中，資料模型往往會相互引用。這些模型之間可能會形成 循環引用
在資料序列化（如 JSON）過程中，這樣的結構會導致 堆疊溢出 (Stack Overflow) 或 無限遞迴，使得應用程式崩潰或無法正常運行。
所以可以在序列化過程中 忽略循環引用。

