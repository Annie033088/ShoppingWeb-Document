技術債?破窗效應? 

Session.Clear 跟 Session.Abandon 清除資料跟清除SessionId的時機點 跟差異

Set @www = (select .. from..)時如果資料是多筆會返回甚麼?報錯 

session 欄位設定 

uuid是甚麼? UID要改一下名稱(最好都全稱)=>UserId

防護快速點擊按鈕(ex:新增..) =>按鈕設定當正在提交時並點擊, 就不執行;或者禁用按鈕或者轉圈圈

不存在的路由，轉移的頁面要修改 

細項目名稱 跟內容標題一致 

導覽頁面的大項目轉導到子項目的功能 可能不需要=>改成類似(必須展開大項目 再點開子項目) 

刪除功能 跳提示窗 

創建使用者失敗 下拉選單有bug 

角色禁用之後 必須檢查 帳號是否可以繼續使用(依據狀態檢查) 1

被踢掉之後需要提示為何被踢掉 

查詢的排序功能 (根據uid, 狀態, 角色) 

禁用跟刪除 之間的關係 (設計的想法) (有可能再刪除帳號的時候, 其他table有連結此帳號的資料時, 可能會出問題) 

實現角色的搜尋功能權限時 有兩種方式 
第一個是只要有分項目功能就給予此權限 
第二個是在功能判斷只要有以下權限就可以使用

下次Demo：修改密碼、角色的刪除、更改功能（細修其他東西）

做code review時先跑流程