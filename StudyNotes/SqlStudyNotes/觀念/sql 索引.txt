索引：
在欄位上設置，可以幫助sql快速找到欄位，以空間換取時間，常常用在不需要update但是需要查詢的資料欄

索引分成：單列索引、複合索引或唯一索引

索引的選擇性和適用場景：
索引是有成本的，包括存儲成本和維護成本。因此，索引應該只在有實際效益的情況下使用。以下是一些典型的使用場景：

1. 查詢頻繁的欄位：如常用於 WHERE 子句的欄位。
2. 排序和分組的欄位：如常用於 ORDER BY 或 GROUP BY 的欄位。
3. 連接的欄位：如兩個表格中用於 JOIN 的欄位。
4. 唯一性約束的欄位：如主鍵（PRIMARY KEY）和唯一鍵（UNIQUE）。
