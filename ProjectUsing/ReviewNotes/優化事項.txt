未來有機會-

程式優化
優先優化：role的分頁
優先優化：發送請求的時候　如果回傳頁面　才會跳轉頁面（導致垃圾資料太多）
優先優化：1.Id可以不顯示	2.搜尋可以不用根據Id
優先優化：統一跳轉頁面時接收資料的方式(是攜帶過去 或是額外再發一個請求取得資料)
優先優化：商品圖片的上傳方式統一
優先優化：統一資料的搜尋及排序使用的方式
優先優化：連動表統一打上全名
優先優化：跳轉頁面統一不攜帶資料(只有一個action(productDetail)

優化：消費點數顯示
優化：運費權限設定
優化：包裝每個response 包括errorcode
優化：抽離重構 - 新增使用者跟修改使用者的時候, 都會到資料庫取得角色Id跟角色名稱, 這個動作可以抽離出來
優化：若跳轉頁面同時需要攜帶資料, 可以直接一次post請求完成(有的部分設定了兩次)

優化：在商品主頁面也設定庫存量預警
優化：每次請求檢查permission, sessionId, 如何減少入庫
優化：修改密碼時，　請使用者重複輸入一次密碼　檢查一不一致
優化：所有可以不用寫在頁面上的css跟js都放在專用資料夾
優化：分類跟查詢可以做複合查詢功能
優化：筆數的資料 可以在後端傳遞給資料庫 而不是在資料庫設定
優化：商品子項目修改或新增圖片時，圖片瀏覽方式沒有刻意設定，可以設定固定顯示大小
優化：查詢使用者的時候不用複用
優化：商品排序
優化：如果有兩個以上的重複程式碼 就可以做抽離重構 
優化：把接口回傳　view, controller 跟api的action分開維護
優化：後端取得資料庫資料的變數，可以考慮用IEnumerable
優化：新增會員欄位-未取件次數, 由5.6->7觸發:會員未取件次數+1
優化：為訂單新增備註的修改時間欄位


版本控制
.gitignore
(需要忽略 不用上傳的到git的檔案)